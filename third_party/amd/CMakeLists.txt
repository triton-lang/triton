include_directories(${CMAKE_CURRENT_SOURCE_DIR}/include)
include_directories(${CMAKE_CURRENT_BINARY_DIR}/include)
include_directories(/opt/rocm/include/hsa)
add_subdirectory(include)
add_subdirectory(lib)
set(ROCM_LIBRARIES /opt/rocm/lib/libhsa-runtime64.so)
if(TRITON_BUILD_PYTHON_MODULE)
  add_triton_plugin(TritonAMD ${CMAKE_CURRENT_SOURCE_DIR}/python/triton_amd.cc LINK_LIBS TritonAMDGPUToLLVM TritonAMDGPUTransforms ${ROCM_LIBRARIES})
endif()
